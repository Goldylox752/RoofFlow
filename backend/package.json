{
  "name": "flow-os-backend",
  "version": "1.0.0",
  "description": "Production backend API for Flow OS (Stripe + Telegram + Supabase automation SaaS)",
  "main": "server.js",
  "license": "MIT",
  "private": true,

  "engines": {
    "node": "20.x"
  },

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests configured\" && exit 0"
  },

  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",

    "@supabase/supabase-js": "^2.49.1",

    "stripe": "^16.0.0",
    "node-telegram-bot-api": "^0.66.0",

    "express-rate-limit": "^7.5.0",

    "helmet": "^7.1.0",
    "compression": "^1.7.4"
  },

  "devDependencies": {
    "nodemon": "^3.1.0"
  },

  "keywords": [
    "saas",
    "backend",
    "stripe",
    "telegram",
    "automation",
    "supabase",
    "billing",
    "webhooks"
  ]
}